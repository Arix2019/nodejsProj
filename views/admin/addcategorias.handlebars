{{#each erros}}
    <div class="alert alert-danger">{{texto}}</div>
{{else}}

{{/each}}

<h3>Adicionar Categorias:</h3>
<div class="card bg-transparent">
    <div class="card-body">
        <form action="/admin/categorias/nova" method="POST">
            <label for="nome">Nome: </label>
                <input class="form-control" type="text" id="nome" name="nome" placeholder="Nome da Categoria">
            <label for="slug">Slug: </label>
                <input class="form-control" type="text" id="slug" name="slug" placeholder="Slug da Categoria">
            <hr>
            <button type="submit" class="btn btn-success mt-1">Criar Categoria</button>
        </form>
        <a href="/admin/categorias/"><button class="btn btn-danger mt-2">Voltar</button></a> 
    </div>
</div>